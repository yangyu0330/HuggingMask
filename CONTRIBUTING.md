# CONTRIBUTING

HuggingMask 팀 공통 기여 규칙입니다. 초보자 기준으로 작성했으며, 팀 운영 문서와 동일한 원칙을 따릅니다.

## 1. 기본 원칙
- 큰 작업을 작은 단위로 나눠서 진행합니다.
- AI가 생성한 코드는 검증 후 반영합니다.
- 설명할 수 없는 코드는 commit하지 않습니다.
- 모듈 경계를 불필요하게 넘는 수정은 피합니다.

## 2. 브랜치 전략
- `main`: 최종 안정 버전 (직접 수정 금지)
- `dev`: 통합 브랜치
- `feature/*`: 개인/기능 작업 브랜치

기본 흐름:
1. `dev` 최신화
2. `feature/*` 브랜치 생성
3. 작업 후 commit/push
4. `feature/*` -> `dev` PR 생성
5. 리뷰 승인 후 merge

## 3. 작업 시작 명령
```bash
git checkout dev
git pull origin dev
git checkout -b feature/기능명
```

## 4. 커밋 메시지 규칙
형식:
```text
[타입] 작업대상: 작업 내용
```

타입:
- `[feat]` 기능 추가
- `[fix]` 버그 수정
- `[refactor]` 구조 개선
- `[docs]` 문서 수정
- `[test]` 테스트 추가/수정
- `[chore]` 기타 설정/정리

예시:
- `[feat] proxy: 모델 요청 라우팅 추가`
- `[fix] analyzer: AST 탐지 누락 케이스 수정`

## 5. PR 규칙
- base는 항상 `dev`
- compare는 항상 `feature/*`
- PR 본문 4개 항목 필수 작성

PR 본문 템플릿:
```text
1. 작업 내용
-

2. 수정 이유
-

3. 테스트 결과
-

4. 확인 요청 사항
-
```

## 6. AI 코드 검증 4단계
1. 코드 직접 읽기: 입력/출력, import, 흐름 확인
2. 실행 확인: 서버/테스트가 실제로 동작하는지 확인
3. 영향 확인: 기존 기능/인터페이스 깨짐 여부 확인
4. 정리 후 commit: 불필요 코드 제거 후 반영

## 7. 리뷰 기준
- 코드가 실행 가능한가
- 기존 기능을 깨뜨리지 않는가
- 작업 설명과 코드가 일치하는가
- 모듈 경계를 넘는 수정이면 이유가 적혀 있는가
- 테스트 결과가 첨부되어 있는가

## 8. 증빙 자료 원칙
아래 자료를 `evidence/`에 주차별로 누적합니다.
- 회의록
- 테스트 로그/결과
- 데모 캡처
- 주요 결정 기록
- 성능 측정 자료

## 9. 금지 사항
- `main` 직접 push
- 승인 없는 임의 merge
- 폴더 구조 임의 변경
- 검증 없는 AI 코드 commit
