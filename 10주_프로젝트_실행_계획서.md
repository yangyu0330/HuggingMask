# HuggingMask 10주 프로젝트 실행 계획서

> **프로젝트명:** AI 모델 공급망 보안 프록시
> 
> 
> **팀명:** HuggingMask
> 
> **기간:** 2026.03.30 ~ 2026.06.07 (10주)
> 
> **팀원:** 양유상(팀장), 박용담, 김민우, 정은미
> 
> **목표:** 개발 완료 후 보고서·논문 단계로 즉시 전환 가능한 상태
> 

---

## 제1부: 운영 가이드

### 1. 프로젝트 구조 개요

전체 10주를 세 개의 Phase로 나눈다.

| Phase | 주차 | 목적 | 핵심 원칙 |
| --- | --- | --- | --- |
| **초기** | 1~2주 | 환경 세팅, 기초 구현, 팀 워크플로 정착 | “도구를 익히고 달리기 전에 신발을 신는다” |
| **중기** | 3~7주 | 핵심 기능 개발 (프록시, 검증엔진, 화이트리스트) | “매주 동작하는 결과물을 만든다” |
| **후기** | 8~10주 | 통합, 테스트, 안정화, 문서화 | “새 기능 금지, 있는 것을 완성한다” |

> [!IMPORTANT]
**8주차부터는 새로운 기능 추가를 금지한다.** 통합·테스트·문서화에만 집중한다.
> 
> 
> 이 규칙을 어기면 10주 안에 완료가 불가능해진다.
> 

---

### 2. 역할 분배

4명의 역할을 모듈 단위로 분배한다. 난이도를 고려하여 핵심 엔진(B)에는 팀장(양유상)이 지원자로 붙고, 거버넌스(C)에는 학습 부담이 적은 대신 문서화를 맡긴다.

| 담당자 | 주 역할 | 상세 모듈 | 난이도 | 비고 |
| --- | --- | --- | --- | --- |
| **양유상** (팀장) | 총괄·통합·config 검증 | 아키텍처 통합, config.json 검증, 통합 테스트, 문서 총괄 | ★★★★ | B 지원, 전체 일정 관리 |
| A | 프록시 서버 | FastAPI 프록시, HF_ENDPOINT 연동, SHA256 캐시, 인증, 라우팅 | ★★★☆ | 프론트엔드 역할 겸임 (개발자 인터페이스) |
| B | 검증 엔진 | pickle 안전 파서, AST 분석, API 화이트리스트, 등급 분류, gVisor 샌드박스 | ★★★★★ | 핵심 연구 기여 영역, 팀장 지원 |
| C | 화이트리스트·거버넌스 | 적응형 화이트리스트 엔진, ML-BOM, Cosign 서명, 감사 로그, 증거자료 관리 | ★★★☆ | 문서화·정산 총괄 겸임 |

**과부하 방지 규칙:**
- B의 작업이 밀릴 경우, 양유상이 AST 분석 또는 등급 분류 로직을 분담한다.
- C가 크롤러 구현에 어려움이 있으면, A이 HTTP 클라이언트 부분을 지원한다.
- 매주 월요일 회의에서 작업량 점검 → 2일 이상 밀린 항목이 있으면 즉시 재분배한다.

---

### 3. 주간 운영 리듬

모든 팀원은 아래 사이클을 매주 반복한다.

| 요일 | 활동 | 산출물 |
| --- | --- | --- |
| **월** | 주간 회의 (30분) — 지난 주 결과 확인, 이번 주 할당 확정, 블로커 공유 | 회의록 (Notion), 이번 주 태스크 카드 |
| **화~목** | 개인 개발 + 일일 진행 보고 (디코) | 코드 커밋, 개인 학습 결과물 |
| **금** | PR 생성 + 주간 증거자료 수집 | PR (feature → dev), 스크린샷/로그, 개인 주간 보고 |
| **토~일** | 개인 숙제 (학습 + 다음 주 사전 준비) | 학습 결과물 (정리 문서 or 코드) |

> [!TIP]
일일 보고는 디코에 1줄로 충분하다:
> 
> 
> `[박용담] SHA256 캐시 모듈 구현 완료, 테스트 3건 통과. 내일: HF 라우팅 연동`
> 

---

### 4. 초보자 학습 병행 전략

**1~2주 집중 학습 (환경·도구):**
- Git/GitHub 워크플로 (branch, PR, merge) — git규칙 페이지 정독
- Docker 기초 (`docker build`, `docker-compose up`)
- Python 개발 환경 (venv, pytest, VSCode)
- AI 코드 생성 도구 올바른 사용법 (프롬프트 작성, 코드 리뷰 요령)

**3~4주 도메인 학습 (보안 기초):**
- pickle 직렬화/역직렬화 원리와 위험성
- AST (Abstract Syntax Tree) 기본 개념
- safetensors 포맷 구조

**5주 이후:**
- 학습보다 구현에 집중. 모르는 것은 AI에 질문하면서 진행.
- 학습은 자기 모듈에 필요한 범위만 just-in-time으로 수행.

---

### 5. 리스크 관리

### 5.1 일정 버퍼

| 리스크 | 발생 확률 | 버퍼 위치 | 대응 전략 |
| --- | --- | --- | --- |
| 개인 일정 지연 (1~2일) | 높음 | 매주 금요일 PR 마감 → 토·일 보충 | 월요일 회의에서 즉시 재분배 |
| 모듈 연동 실패 | 중간 | 7주차에 1주 통합 버퍼 확보 | 인터페이스(입출력 포맷) 4주차에 확정·동결 |
| gVisor 환경 구축 실패 | 중간 | 5~6주차에 시도, 실패 시 Docker fallback | gVisor 없이 Docker + seccomp만으로 대체 가능 |
| 특정 팀원 이탈/불참 | 낮음 | 모듈 간 코드 리뷰로 최소 2명이 내용 파악 | 페어 프로그래밍 1회/2주, 코드 문서화 필수 |
| 캡스톤 행정 일정 충돌 | 중간 | C가 학사 일정 추적 담당 | 2주 전 사전 확인, 발표·보고서 일정 역산 |

### 5.2 재작업 기간

- **8주차 전반:** 통합 테스트에서 발견된 버그 수정 전용 기간
- **9주차:** 시나리오 테스트 실패 시 재작업 기간 (2일 분량 확보)
- **10주차 전반:** 최종 수정 기한. 10주차 수요일 이후에는 코드 동결(Code Freeze)

### 5.3 범위 축소 우선순위 (일정 밀릴 경우)

일정이 밀릴 경우, 아래 순서대로 범위를 축소한다:

| 우선순위 | 축소 대상 | 축소 방법 | 영향 |
| --- | --- | --- | --- |
| 1순위 (먼저 축소) | Cosign/Sigstore 서명 | 해시 기반 무결성만 확보, 서명은 Future Work | 낮음 |
| 2순위 | 적응형 화이트리스트 크롤러 | 수동 화이트리스트 YAML 관리로 대체 | 중간 |
| 3순위 | gVisor 샌드박스 (Path B) | Docker 컨테이너 격리로 대체 | 중간 |
| 4순위 | 오탐 피드백 루프 | 수동 리뷰만 유지 | 낮음 |
| **절대 축소 불가** | 프록시 서버, pickle Path A, AST+API 화이트리스트, 등급 분류 | — | — |

---

### 6. 증거자료 수집 및 문서 관리

개발 중부터 아래 폴더를 생성하고, 매주 자료를 수집한다.

```
evidence/
├── week01/ ~ week10/    # 주차별 스크린샷, 로그, 데모 캡처
├── meetings/            # 회의록 PDF/MD, 참석자 기록
├── tests/               # pytest 결과, 수동 테스트 표
├── demo/                # 5개 시나리오 입력·명령어·출력
├── perf/                # 처리 시간, 비교표
└── budget/              # 영수증, 회의비 증빙
```

**매주 반드시 남기는 항목:** 회의록, 주요 결정 기록, 아키텍처 변경 기록, 테스트 결과, 버그 및 수정 내역, 성능 수치, 데모 스크린샷, known limitations 정리, 참고문헌 후보 목록

> [!IMPORTANT]
8주차부터는 **보고서·논문에 쓸 그림과 표를 따로 표시**해 둔다. 이 작업을 C가 주도한다.
> 

---

### 8. 10주차 종료 시 인계 패키지 (체크리스트)

10주차 금요일(6월 5일)까지 아래가 모두 준비되어야 한다:

- [ ]  `docs/release_notes_v1.0.md`
- [ ]  `docs/handoff_for_report_and_paper.md`
- [ ]  `docs/final_demo_script.md`
- [ ]  `docs/final_acceptance_sheet.md`
- [ ]  README 최종판
- [ ]  실행 가이드 (`docs/setup_guide.md`)
- [ ]  아키텍처 설명 자료 (다이어그램 포함)
- [ ]  테스트 결과표
- [ ]  AI 사용 추적표
- [ ]  evidence index (증거자료 목차)
- [ ]  캡스톤 제출 서류: 결과보고서, 정산 내역서, 회의록, 영수증, 만족도 조사

---

## 제2부: 주차별 계획표

### Week 1 (03/30 ~ 04/05) — 환경 구축 + 팀 워크플로 정착

**주차 목표:** 모든 팀원이 동일한 개발 환경에서 코드를 작성·push·PR할 수 있는 상태를 만든다

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | GitHub 레포 생성, branch 규칙 설정 (main/dev/feature), PR 템플릿 작성, 팀원에게 Git 워크플로 시연 1회 | 팀원 4명 모두 feature → dev PR 1건 성공 |
| A | Python venv 세팅 가이드 작성, FastAPI 빈 프로젝트 생성 (`/health` 엔드포인트), Docker Compose 초안 작성 | `docker-compose up`으로 FastAPI 서버 `200 OK` 확인 |
| B | Python `ast` 모듈 학습, `ast.parse()` / `ast.walk()` 샘플 코드 작성, pickle 직렬화 원리 학습 (위험한 opcode 목록 정리) | `sample.py`를 AST로 파싱하여 import/함수호출 추출하는 스크립트 동작 |
| C | 프로젝트 Notion 페이지 구성 (회의록, 태스크 보드, AI 추적표), evidence 폴더 구조 생성, 캡스톤 학사 일정 정리 | Notion 페이지 링크 공유, evidence/ 폴더 구조 커밋 |

**학습 과제 (전원):**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 양유상 | Docker Compose로 2개 컨테이너 통신 예제 작성 (FastAPI ↔︎ Redis 또는 간단한 서비스) | `docker-compose.yml` + 동작 스크린샷 |
| A | FastAPI 공식 튜토리얼 완료 (Path Parameters, Request Body, Response Model까지) | 학습 정리 MD 파일 |
| B | Python pickle 위험성 실습: `__reduce__`로 `os.system('echo hacked')` 실행하는 악성 pickle 생성 및 로드 | 악성 pickle 생성/실행 코드 + 결과 스크린샷 |
| C | HuggingFace `from_pretrained()` 사용법 학습, safetensors vs pickle 차이 정리 | 비교표 문서 (A4 1장 분량) |

---

### Week 2 (04/06 ~ 04/12) — 프록시 뼈대 + 파일 분류기

**주차 목표:** 프록시 서버가 HuggingFace 요청을 가로채서 파일 유형별로 분류하는 기본 흐름 완성

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | 전체 모듈 인터페이스 정의서(input/output JSON 스키마) 초안 작성, config.json 검증 모듈 설계 시작 | 인터페이스 정의서 PR → dev 머지 |
| A | HF_ENDPOINT 기반 프록시 라우팅 구현, 파일 유형 분류기 (safetensors/pickle/py/json 판별), 가짜 HF 서버(테스트용 Mock) 구현 | `HF_ENDPOINT=http://localhost:8000`으로 `from_pretrained()` 호출 시 프록시 로그 출력 확인 |
| B | safetensors 검증 모듈 구현 (해시 검증 + 메타데이터 검사), pickletools 기반 opcode 추출 프로토타입 | safetensors 파일 입력 → 해시 검증 → PASS/FAIL 출력 |
| C | 화이트리스트 DB 스키마 설계 (SQLite), `approved_apis` / `pending_apis` 테이블 생성, 초기 화이트리스트 YAML 파일 작성 (torch.nn.*, torch.Tensor.* 등) | DB 생성 스크립트 + 초기 화이트리스트 YAML (100개 이상 API) |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 양유상 | config.json의 `auto_map`, `trust_remote_code` 필드가 어떻게 코드 실행을 트리거하는지 HuggingFace 소스코드 분석 | 분석 정리 문서 (코드 흐름도 포함) |
| A | HuggingFace Hub API 학습 (`huggingface_hub` 라이브러리 구조, 모델 다운로드 흐름 분석) | API 호출 흐름도 |
| B | pickle opcode 완전 분류표 작성 (PROTO~STOP 까지 각 opcode 역할, 데이터용/실행용 구분) | opcode 분류표 (표 형태) |
| C | PyTorch 공식 문서에서 `torch.nn` 하위 API 50개 수동 추출 연습 | 추출 결과 CSV/JSON 파일 |

---

### Week 3 (04/13 ~ 04/19) — pickle 안전 파서(Path A) + AST 분석기

**주차 목표:** 레거시 pickle 모델의 안전 파서가 동작하고, 커스텀 코드의 위험 패턴을 탐지하는 AST 분석기 초안 완성

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | config.json 스키마 검증 모듈 구현 (`auto_map`, `trust_remote_code`, `custom_pipelines` 탐지), 참조 .py 파일 → 코드 검증 경로로 라우팅 연결 | 악성 config.json 입력 시 트리거 필드 탐지 + 경고 출력 |
| A | SHA256 캐시 모듈 구현, 캐시 히트 시 즉시 반환 로직, 정책 버전 기반 캐시 무효화 로직 | 동일 모델 2회 요청 시 2번째는 캐시에서 즉시 반환 확인 |
| B | pickle 안전 파서 구현 (opcode 화이트리스트 기반, 위험 opcode 차단), 정상 pickle → safetensors 변환 구현, AST 분석기 1단계 (import 추출, 위험 함수 탐지: eval/exec/open/**import**) | 정상 pickle 파일 → safetensors 변환 성공, 악성 pickle → REDUCE opcode 차단 |
| C | 초기 화이트리스트 완성 (torch.nn.*, torch.nn.functional.*, transformers.* 주요 API), 화이트리스트 조회 API 구현 (`is_allowed(api_path) → bool`) | `is_allowed("torch.nn.Linear")` → True, `is_allowed("torch.load")` → False |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 양유상 | gVisor(runsc) 설치 및 Docker와 연동 테스트 (김민우 5주차 사용 대비 사전 검증) | 설치 가이드 + 동작 확인 스크린샷 |
| A | FastAPI 미들웨어 패턴 학습 (인증, 로깅, 에러 처리) | 미들웨어 적용 코드 샘플 |
| B | getattr, type() 3인자, chr/ord 난독화 패턴을 AST로 탐지하는 방법 연구 | 각 패턴별 AST 노드 구조 정리 문서 |
| C | ML-BOM(Machine Learning Bill of Materials) 표준 포맷 조사 (CycloneDX ML-BOM 등) | ML-BOM 포맷 비교 정리 문서 |

---

### Week 4 (04/20 ~ 04/26) — AST+API 화이트리스트 연동 + 인터페이스 동결

**주차 목표:** AST 분석 → API 화이트리스트 검증이 연결되어 커스텀 코드의 안전 여부를 자동 판정. **모듈 간 인터페이스를 이번 주에 확정·동결한다.**

> [!WARNING]
**인터페이스 동결 (Interface Freeze):** 4주차 금요일 이후 모듈 간 입출력 포맷 변경 금지. 변경이 반드시 필요하면 팀장 승인 + 관련 모듈 담당자 동의 후 진행.
> 

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | 인터페이스 정의서 최종 확정, 검증 리포트 JSON 스키마 설계 (스캔 결과, 등급, 해시, 타임스탬프 포함), config 검증 모듈 → 코드 검증 모듈 연동 테스트 | 인터페이스 정의서 v1.0 → dev 머지 + 전원 동의 |
| A | 프록시 → 검증 엔진 호출 인터페이스 구현, 파일 분류 후 적절한 검증 모듈로 라우팅하는 디스패처 로직 | safetensors/pickle/py/json 파일 각각이 올바른 검증 모듈로 라우팅됨을 로그로 확인 |
| B | AST 분석기 2단계(API 수준 화이트리스트 검증), 미등록 API 탐지 시 Pending List 등록 로직, AST + 화이트리스트 통합 파이프라인 완성 | 정상 코드 → PASS, 위험 함수 포함 코드 → BLOCK, 미등록 API → PENDING 출력 |
| C | Pending List 등록/조회 API 구현, 네임스페이스 기반 자동 분류 로직 구현 (자동승인/조건부/수동/차단), 검증된 조직(Verified Org) 사용 여부 태그 설계 | 미등록 API 입력 → 자동 분류 결과 + Pending List 등록 확인 |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 양유상 | pytest 기반 테스트 작성법 학습, 프로젝트의 테스트 디렉토리 구조 설계 | `tests/` 디렉토리 구조 + 샘플 테스트 3건 |
| A | API 키 인증 미들웨어 구현 학습 (FastAPI Depends 패턴) | 인증 미들웨어 코드 |
| B | 실제 HuggingFace 모델의 커스텀 코드 3개 다운받아 등급 분류 수동 연습 | 3개 모델의 등급 판정 결과 + 근거 정리 |
| C | PyTorch 공식 문서 크롤링 프로토타입 (requests + BeautifulSoup) | 크롤링 스크립트 + 추출 결과 10개 API |

---

### Week 5 (04/27 ~ 05/03) — 등급 분류 엔진 + 샌드박스

**주차 목표:** 커스텀 코드 복잡도 등급(A/B-1/B-2/C) 자동 판정이 동작하고, gVisor 또는 Docker 격리 환경에서 코드 실행 테스트가 가능한 상태

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | 등급 A 처리 모듈 구현: PretrainedConfig 코드 → AST 추출 → 코드 재생성, config 검증 고도화 (tokenizer_config.json 추가) | MiniMaxM2 configuration 파일 → 등급 A 판정 → 재생성된 코드가 원본과 동일 인터페이스 |
| A | 내부 인증 모듈(API 키) 구현, 에러 핸들링 미들웨어 추가, 프록시 로깅 표준화 | API 키 없이 접근 시 401, 모든 요청·응답 로그 기록 |
| B | 등급 분류 엔진 구현 (A/B-1/B-2/C 자동 판정), 등급 B-1 제한 런타임 구현(위험 빌트인 제거 + import 제한), gVisor 또는 Docker 격리 환경 구축 (등급 B-2/C용) | 등급 A~C 각각 1개씩 4건 테스트 코드로 등급 판정 정확도 확인 |
| C | 공식 문서 크롤러 모듈 구현 (PyTorch/Transformers 공식 문서에서 API 추출), 안전도 자동 분류 규칙 적용 | 크롤러 실행 → 신규 API 목록 추출 → 네임스페이스 분류 결과 출력 |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 양유상 | Cosign/Sigstore 서명 개념 학습, 설치 가이드 준비 | Cosign 서명/검증 명령어 정리 문서 |
| A | FastAPI + Docker Compose 멀티 컨테이너 통신 패턴 학습 (서비스 디스커버리) | 프록시 → 분석 엔진 컨테이너 통신 테스트 |
| B | gVisor(runsc) 환경에서 Python 코드 실행 테스트 (network=none, read-only FS) | gVisor 환경 구축 + 간단한 Python 실행 확인 |
| C | append-only 감사 로그 설계 패턴 조사 (해시 체인 방식) | 감사 로그 스키마 설계문서 |

---

### Week 6 (05/04 ~ 05/10) — End-to-End 파이프라인 v0.1

**주차 목표:** 프록시 → 파일 분류 → 검증(safetensors/pickle/코드/config) → 결과 반환의 전체 흐름이 1개 시나리오에서 end-to-end로 동작

> [!IMPORTANT]
**중간 체크포인트:** 이 주에 “정상 safetensors 모델이 프록시를 통해 검증 후 통과”하는 시나리오가 동작하지 않으면, 즉시 일정 조정 회의를 소집한다.
> 

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | 프록시 ↔︎ 검증엔진 ↔︎ 화이트리스트 모듈 통합 연동, e2e 테스트 시나리오 1번(정상 safetensors) 달성 | `from_pretrained()` → 프록시 → safetensors 검증 → 캐시 저장 → 개발자에게 반환 성공 |
| A | 내부 모델 저장소(로컬 파일 기반) 구현, 승인 모델 저장·조회, 가짜 HF 서버에 악성 테스트 모델 추가 | 검증 통과 모델이 내부 저장소에 저장되고, 재요청 시 캐시에서 즉시 반환 |
| B | 등급 B-2/C 샌드박스 실행 파이프라인 구현, strace 로그 수집 + 자동 이상 탐지 규칙 적용, Path A + Path B 결과 비교 로직 구현 | 정상 pickle → Path A(safetensors 변환) + Path B(샌드박스 실행) → 비교 일치 → PASS |
| C | ML-BOM 생성 모듈 구현 (모델 메타데이터 + 검증 결과 + 해시), append-only 감사 로그 구현 | 검증 완료 시 ML-BOM JSON 파일 자동 생성 확인 |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 전원 공통 | 5개 시나리오별 테스트 데이터 준비 분담 (정상 safetensors, 정상 pickle, 악성 pickle, 악성 코드, 악성 config) | 각자 1~2개 시나리오 입력 파일 제작 |

---

### Week 7 (05/11 ~ 05/17) — 5개 시나리오 완성 + 통합 버퍼

**주차 목표:** 5개 데모 시나리오가 모두 end-to-end로 동작. 이번 주가 기능 개발의 마지막 기회.

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | e2e 테스트 시나리오 2~5번 순차 달성, 통합 버그 수정 조율, 검증 리포트 생성 기능 통합 | 5개 시나리오 모두 성공 (시나리오별 통과/차단 결과 일치) |
| A | 데모용 가짜 HF 서버 완성 (5개 시나리오 모델 제공), Docker Compose 완성 (프록시 + 분석엔진 + Mock HF), 프록시 에러 핸들링 보강 | `docker-compose up` 한 번에 전체 시스템 실행, 5개 시나리오 모두 가짜 HF에서 모델 제공 |
| B | 등급별 처리 파이프라인 엣지 케이스 보강, 검증 실패 시 에러 메시지 개선, 성능 1차 측정 (시나리오별 처리 시간 기록) | 검증 실패 시 구체적 사유(어떤 opcode/API/패턴에서 실패)를 차단 메시지에 포함 |
| C | 보안 담당자 리뷰 UI 또는 CLI 구현 (Pending List 조회 → 승인/거부/보류), 오탐 피드백 수신 API 구현, Cosign 서명 연동 시도 (실패 시 해시만 유지) | Pending List에서 API 승인 → 화이트리스트 자동 반영 확인 |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 전원 공통 | 자기 모듈의 단위 테스트 최소 5건 작성 | `tests/` 폴더에 테스트 코드 커밋 |

---

### Week 8 (05/18 ~ 05/24) — 안정화 + 통합 테스트

> [!CAUTION]
**8주차부터 새로운 기능 추가 금지.** 있는 기능을 안정화하고, 테스트하고, 문서화한다.
> 

**주차 목표:** 전체 시스템의 버그를 수정하고, 자동화된 테스트를 완성하며, 성능 측정을 완료한다

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | 통합 테스트 시나리오 작성 및 실행 (pytest), 오탐/미탐 케이스 정리, 아키텍처 다이어그램 최종본 작성 | 통합 테스트 10건 이상 PASS, 아키텍처 다이어그램 PNG/SVG |
| A | 프록시 안정화 (타임아웃, 재시도, 에러 복구), Docker Compose 최종 안정화, README 초안 작성 | 1시간 연속 운영 시 크래시 없음 |
| B | 검증 엔진 엣지 케이스 테스트 (비정상 입력, 대용량 파일, 인코딩 문제), 성능 측정 (시나리오별 처리 시간 3회 반복 + 평균·표준편차) | 성능 측정 결과표 작성 완료 |
| C | 감사 로그 무결성 검증 테스트, 모든 모듈의 known limitations 수집·정리, 보고서용 그림·표 목록 작성 시작 | known limitations 문서, 보고서용 그림 후보 10개 이상 표시 |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 전원 공통 | 자기 모듈 코드에 docstring과 주석 추가 (함수/클래스 수준) | 코드 정리 커밋 |

---

### Week 9 (05/25 ~ 05/31) — 데모 준비 + 문서화

**주차 목표:** 발표용 데모가 안정적으로 반복 실행 가능하고, 주요 문서의 초안이 완성된다

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | `final_demo_script.md` 작성 (5개 시나리오 순서, 실행 명령어, 예상 결과), 데모 리허설 2회 실행, `handoff_for_report_and_paper.md` 초안 | 데모 2회 연속 성공 (동일 결과) |
| A | 실행 가이드 (`setup_guide.md`) 작성, Docker Compose 원커맨드 실행 검증, README 완성 | 클린 환경에서 README 따라 하면 시스템 구동 가능 |
| B | `release_notes_v1.0.md` 작성 (기능 목록, 제한 사항, 알려진 이슈), 검증 엔진 최종 안정화 | release notes 완성 |
| C | 캡스톤 결과보고서 초안 작성, 정산 내역서 정리 (영수증·증빙 확인), 증거자료 인덱스 작성 | 결과보고서 80% 초안, 정산 서류 완성 |

**학습 과제:**

| 팀원 | 개인 숙제 | 제출물 |
| --- | --- | --- |
| 전원 공통 | 발표 자료 각자 담당 슬라이드 초안 (팀장이 분배) | 각자 PPT 슬라이드 3~5장 |

---

### Week 10 (06/01 ~ 06/07) — 최종 마감 + 인계 패키지 완성

> [!CAUTION]
**코드 프리즈: 10주차 수요일(6/3) 이후 코드 변경 금지.** 목~금은 문서화와 발표 준비에만 집중.
> 

**주차 목표:** 모든 산출물 완성, 인계 패키지 체크리스트 100% 달성, 발표 리허설 완료

| 담당자 | 작업 내용 | 완료 기준 |
| --- | --- | --- |
| **양유상** | 인계 패키지 체크리스트 최종 확인, `final_acceptance_sheet.md` 작성, 발표 자료 통합·리허설 총괄, 발표 리허설 2회 | 인계 패키지 100% 완성, 발표 15분 이내 완료 |
| A | Docker 이미지 최종 빌드·태깅, README 최종 검수, 실행 가이드로 팀원 1명에게 신규 구동 테스트 요청 | 다른 팀원이 README만 보고 시스템 구동 성공 |
| B | 테스트 결과표 최종본 작성, 성능 비교표 최종 정리, 발표 데모 최종 확인 | 테스트 결과표 + 성능표 최종본 |
| C | 결과보고서 최종본 완성, 정산 내역서 최종 제출 준비, AI 사용 추적표 최종 정리, evidence index 완성, 만족도 조사 준비 | 캡스톤 제출 서류 세트 100% 완성 |

**학습 과제:** 없음. 최종 마감에 집중.

---

## 제3부: 마일스톤 요약표

| 마일스톤 | 달성 시점 | 판정 기준 | 미달 시 대응 |
| --- | --- | --- | --- |
| **M0 — 환경 완비** | 1주차 금 | 팀원 4명 PR 성공, Docker 기동 | 토·일 보충 작업 |
| **M1 — 프록시 기본 동작** | 2주차 금 | `from_pretrained()` → 프록시 로그 확인 | 3주차 월~화 보충 |
| **M2 — Path A + AST 기본 동작** | 3주차 금 | pickle → safetensors 변환, AST 위험 함수 탐지 | 김민우·양유상 페어 프로그래밍 |
| **M3 — 인터페이스 동결** | 4주차 금 | 인터페이스 정의서 v1.0 확정 | 주말까지 연장 불가 — 강제 확정 |
| **M4 — 등급 분류 동작** | 5주차 금 | A/B-1/B-2/C 각 1건 판정 성공 | 6주차에 B-2/C 범위 축소 검토 |
| **M5 — E2E v0.1** | 6주차 금 | 시나리오 1번(정상 safetensors) e2e 성공 | **긴급 일정 조정 회의 소집** |
| **M6 — 5개 시나리오 완성** | 7주차 금 | 5개 시나리오 전부 PASS/BLOCK 정확 | 8주차 전반에 보충 (안정화 기간 축소) |
| **M7 — 안정화 완료** | 8주차 금 | 통합 테스트 10건+ PASS, 성능 측정 완료 | 9주차 전반에 보충 |
| **M8 — 문서화 완료** | 9주차 금 | 데모 2회 성공, 문서 초안 완성 | 10주차 월~화 보충 |
| **M9 — 최종 제출** | 10주차 금 | 인계 패키지 100%, 캡스톤 서류 완성 | 해당 주 야간 작업 (최후 수단) |

---

## 제4부: 주차별 증거자료 수집표

| 주차 | 회의록 | 스크린샷/로그 | 테스트 결과 | 데모 영상 | 결정 기록 |
| --- | --- | --- | --- | --- | --- |
| W1 | ✅ | 환경 세팅 스크린샷 | — | — | 브랜치 전략 결정 |
| W2 | ✅ | 프록시 로그 | safetensors 검증 | — | 인터페이스 초안 |
| W3 | ✅ | Path A 실행 로그 | pickle 변환, AST 탐지 | — | opcode 화이트리스트 확정 |
| W4 | ✅ | 등급 판정 로그 | API 화이트리스트 테스트 | — | **인터페이스 동결** |
| W5 | ✅ | 샌드박스 실행 로그 | 등급 분류 4건 | — | gVisor/Docker 결정 |
| W6 | ✅ | e2e 흐름 로그 | 시나리오 1 성공 | 시나리오 1 캡처 | 범위 축소 여부 결정 |
| W7 | ✅ | 5개 시나리오 로그 | 5개 시나리오 전체 | 5개 시나리오 캡처 | 기능 동결 결정 |
| W8 | ✅ | 통합 테스트 로그 | 통합 10건+ | — | 성능 측정 결과 |
| W9 | ✅ | 데모 리허설 녹화 | 최종 테스트 | 데모 영상 | 문서 확정 |
| W10 | ✅ | 최종 시스템 캡처 | 최종 결과표 | 최종 데모 | 코드 프리즈 |

---

> [!NOTE]
이 계획서는 살아있는 문서이다. 매주 월요일 회의에서 진행 상황을 반영하여 갱신하되, 마일스톤 날짜와 인터페이스 동결 시점은 특별한 사유가 없는 한 변경하지 않는다.
>