# HuggingMask 팀 GitHub 운영 규칙 및 개발 증빙 관리 가이드

## 문서 목적

이 문서는 **HuggingMask 팀이 GitHub에서 실제로 작업할 때 반드시 따라야 하는 규칙**을 정리한 운영 문서다.  
대상은 **팀 프로젝트 경험이 많지 않은 팀원**이며, **AI를 활용해 코드를 작성하는 환경**을 전제로 한다.

이 문서의 목적은 아래 4가지다.

1. Git 충돌과 브랜치 혼선을 줄인다.
2. AI가 만든 코드를 그대로 반영하는 위험을 줄인다.
3. 누가 무엇을 왜 수정했는지 기록이 남게 한다.
4. 개발 종료 후 바로 보고서·논문 작성 단계로 넘어갈 수 있도록 증빙 자료까지 같이 관리한다.

---

# 1. 기본 원칙

## 1-1. 작업은 반드시 작은 단위로 나눈다

AI에게 한 번에 큰 기능 전체를 맡기면,

- 구조가 뒤섞이기 쉽고
- 불필요한 코드가 많이 생기고
- 어디서 문제가 났는지 찾기 어려워진다.

따라서 작업은 반드시 **작은 단계**로 나눠서 진행한다.

예시:

- 프록시 전체 구현 요청 ❌
- 인증 미들웨어만 구현 ✅
- 요청 라우팅만 구현 ✅
- 캐시 조회 함수만 구현 ✅

---

## 1-2. 설명할 수 없는 코드는 commit하지 않는다

AI가 생성한 코드라도 아래를 **직접 설명할 수 있어야 한다**.

- 이 함수가 무슨 역할인지
- 입력과 출력이 무엇인지
- 왜 이렇게 구성했는지
- 기존 코드와 어떻게 연결되는지

즉, **“돌아가니까 그냥 넣는다”는 금지**한다.

---

## 1-3. 폴더 구조는 임의로 바꾸지 않는다

현재 프로젝트는 아래와 같은 **모듈 중심 구조**를 기준으로 진행한다.

```text
proxy
analyzer
whitelist
sandbox
```

이 구조가 섞이면

- 같은 파일을 여러 명이 동시에 건드리게 되고
- Git 충돌이 많아지고
- 책임 구분이 흐려진다.

따라서 **폴더 구조를 바꾸거나 모듈 위치를 옮길 때는 반드시 사전 허락을 받고 변경**한다.

---

## 1-4. 모듈 중심으로 작업한다

아래 모듈을 기준으로 작업을 분리한다.

- `proxy`
- `analyzer`
- `whitelist`
- `sandbox`

원칙은 다음과 같다.

- 한 사람이 한 시점에 한 모듈 중심으로 작업한다.
- 여러 사람이 같은 파일을 동시에 수정하지 않는다.
- 공통 파일 수정이 필요하면 먼저 팀장과 공유한다.
- 모듈 경계를 넘는 수정은 PR 설명에 반드시 이유를 적는다.

---

# 2. AI가 생성한 코드 검증 원칙

AI가 생성한 코드는 **그대로 commit하지 않는다. 반드시 검증 후 commit**한다.

검증은 최소 4단계로 진행한다.

## 2-1. 1단계 — 코드 직접 읽기

아래를 직접 확인한다.

- 어떤 함수가 추가되었는지
- 입력과 출력이 무엇인지
- 기존 코드와 연결 방식이 맞는지
- 이상한 import가 없는지
- 불필요한 코드가 없는지

체크 예시:

- 사용하지 않는 import가 있는가
- 갑자기 전역 변수나 하드코딩이 생겼는가
- 기존 인터페이스를 깨는 함수명이 들어갔는가
- 디버깅용 print가 남아있는가

---

## 2-2. 2단계 — 실행 확인

아래를 확인한다.

- 프로그램이 실제로 실행되는지
- 문법 오류가 없는지
- 최소한 해당 기능이 에러 없이 동작하는지

확인 예시:

- 서버가 정상 기동되는가
- 테스트용 입력에 대해 함수가 정상 반환하는가
- import error, syntax error, path error가 없는가

---

## 2-3. 3단계 — 기존 기능 영향 확인

새 코드가 기존 기능을 깨뜨리지 않는지 확인한다.

- 다른 함수나 모듈을 깨뜨리지 않는지
- 파일명, 함수명, 경로가 현재 구조와 맞는지
- 기존 로직과 중복되지는 않는지

체크 예시:

- 기존 API 응답 형식이 바뀌지 않았는가
- 파일 경로가 로컬 환경 기준으로 고정되지 않았는가
- 비슷한 유틸 함수가 이미 있는데 중복 생성하지 않았는가

---

## 2-4. 4단계 — 정리 후 commit

검증이 끝나면 그대로 올리지 말고 정리한다.

- 필요 없는 주석 삭제
- 중복 코드 제거
- 변수명/함수명 정리
- 커밋 메시지에 작업 내용을 명확히 작성

즉, **AI 출력물 정리 → 검증 → commit** 순서로 진행한다.

---

# 3. Git 브랜치 운영 규칙

## 3-1. 브랜치 구조

우리 팀은 아래 구조로 운영한다.

```text
main        → 절대 직접 수정 금지 (배포용)
dev         → 통합 브랜치
feature/*   → 개인 작업 브랜치
```

예시:

```text
feature/ast-parser
feature/proxy-server
feature/whitelist-engine
```

원칙:

- 작업은 **무조건 feature 브랜치에서 시작**
- 작업 결과는 **dev로 PR**
- `main`은 마지막에만 반영
- `main`에 직접 push 금지
- `dev`에도 직접 push하지 않는 것을 원칙으로 한다

---

## 3-2. 가장 간단한 PR 실사용 요약

팀원들은 아래 순서만 기억하면 된다.  
처음 할 때는 노션 내용 따라가면서 진행한다.

```text
1. dev 최신화
2. feature 브랜치 생성
3. 작업 후 commit
4. GitHub에 push
5. Pull Request 생성 (feature → dev)
6. 작업 내용 / 이유 / 테스트 결과 작성
7. 리뷰 요청
8. 수정 요청 오면 같은 브랜치에서 수정 후 다시 push
9. 승인받고 merge
```

---

# 4. 실제 작업 절차

## 4-1. 작업 시작 전

항상 `dev`를 최신 상태로 맞춘 뒤 새 브랜치를 만든다.

```bash
git checkout dev
git pull origin dev
git checkout -b feature/기능명
```

예시:

```bash
git checkout dev
git pull origin dev
git checkout -b feature/ast-parser
```

---

## 4-2. 작업 후 기본 절차

```bash
git add .
git commit -m "[feat] analyzer: AST 분석 기능 추가"
git push origin feature/기능명
```

전체 예시:

```bash
git checkout dev
git pull origin dev
git checkout -b feature/ast-parser

# 작업 수행

git add .
git commit -m "[feat] analyzer: AST 분석 기능 추가"
git push origin feature/ast-parser
```

그 다음 GitHub에서 **feature → dev** PR을 생성한다.

---

# 5. 커밋 메시지 작성 규칙

## 5-1. 형식

커밋 메시지는 아래 형식으로 작성한다.

```text
[타입] 작업 대상(어디를): 무엇을 했는지
```

예시:

- `[feat] proxy: 모델 요청을 프록시 서버로 우회하는 기능 추가`
- `[fix] proxy: 요청 헤더 누락으로 인한 인증 오류 수정`
- `[refactor] analyzer: AST 탐지 로직 함수 분리`
- `[docs] whitelist: 정책 문서 초안 정리`
- `[test] sandbox: pickle 차단 테스트 추가`

---

## 5-2. 타입 종류

- `[feat]` 기능 추가
- `[fix]` 버그 수정
- `[refactor]` 구조 개선 (동작 변화 없음)
- `[docs]` 문서 수정
- `[test]` 테스트 코드 추가/수정
- `[chore]` 기타 설정/잡일

---

## 5-3. 작성 원칙

좋은 커밋 메시지는 아래를 만족해야 한다.

- 한국어로 읽었을 때 바로 이해 가능할 것
- 어디를 수정했는지 드러날 것
- 무엇을 했는지 드러날 것
- 너무 추상적이지 않을 것

나쁜 예시:

- `수정`
- `버그 고침`
- `업데이트`
- `기능 추가`

좋은 예시:

- `[fix] proxy: 토큰 검증 누락으로 인한 인증 실패 수정`
- `[feat] whitelist: 허용 API 목록 JSON 로더 추가`
- `[refactor] sandbox: 실행 로그 저장 경로 분리`

---

# 6. PR(Pull Request) 규칙

PR은 내가 작업한 내용을 **바로 합치지 않고**  
먼저 팀장이 내용을 확인한 뒤 `dev` 브랜치에 반영하기 위한 절차다.

즉, PR은 단순 Git 기능이 아니라 **팀 프로젝트의 검수 절차**다.

---

## 6-1. PR이 필요한 이유

PR을 사용하는 이유는 다음과 같다.

- 내가 수정한 코드가 다른 기능에 문제를 일으키는지 확인하기 위해
- 팀장이 작업 내용을 빠르게 파악하기 위해
- 누가 어떤 이유로 어떤 코드를 수정했는지 기록으로 남기기 위해
- AI가 생성한 코드의 오류를 바로 `main`이나 `dev`에 반영하지 않기 위해

---

## 6-2. PR 생성 전 반드시 확인할 것

PR을 만들기 전 아래를 먼저 확인한다.

1. 현재 작업 브랜치가 `feature/기능명`인지 확인
2. 작업한 코드가 실제로 실행되는지 확인
3. 불필요한 파일이 포함되지 않았는지 확인
4. `dev` 브랜치를 최신 상태로 pull 했는지 확인
5. 충돌(conflict)이 나는지 확인

확인 예시:

```bash
git checkout dev
git pull origin dev
git checkout feature/ast-parser
git merge dev
```

충돌이 없다면 계속 진행한다.  
충돌이 있으면 먼저 해결한 뒤 push한다.

---

## 6-3. PR 생성 실제 순서

### 1단계. 작업 브랜치 push

```bash
git add .
git commit -m "[feat] analyzer: AST 위험 함수 탐지 기능 추가"
git push origin feature/ast-parser
```

### 2단계. GitHub에서 PR 생성

GitHub 저장소에 들어가면 방금 push한 브랜치에 대해  
`Compare & pull request` 버튼이 보인다. 이 버튼을 클릭한다.

버튼이 안 보이면 아래 방식으로 직접 생성한다.

1. GitHub 저장소 접속
2. 상단 메뉴에서 **Pull requests** 클릭
3. **New pull request** 클릭
4. base 브랜치: `dev`
5. compare 브랜치: `feature/ast-parser`

즉,  
**내 작업 브랜치(feature)를 dev에 합치는 요청**을 만드는 것이다.

---

## 6-4. base / compare 브랜치 설정 규칙

초보자가 가장 많이 헷갈리는 부분이다.

- **base**: 합쳐질 대상 브랜치
- **compare**: 내가 작업한 브랜치

우리 팀 규칙은 아래와 같다.

- base: `dev`
- compare: `feature/기능명`

예시:

- base: `dev`
- compare: `feature/proxy-cache`

이 설정이 반대로 되면 안 된다.

---

## 6-5. PR 제목 작성 규칙

PR 제목은 한눈에 작업 내용을 알 수 있게 적는다.

형식:

```text
[타입] 작업 요약
```

예시:

```text
[feat] AST 위험 함수 탐지 기능 추가
[fix] 프록시 캐시 경로 오류 수정
[docs] 화이트리스트 정책 문서 정리
[refactor] 분석 엔진 구조 분리
```

핵심 기능에 영향을 주는 작업은 앞에 `[CORE]`를 붙인다.

예시:

```text
[CORE][feat] whitelist 판정 로직 수정
[CORE][fix] sandbox 실행 정책 오류 수정
```

---

## 6-6. PR 본문 작성 규칙

PR 본문은 반드시 작성한다.  
짧아도 되지만 아래 4개는 꼭 적는다.

### PR 본문 양식

```text
1. 작업 내용
- 무엇을 수정했는지

2. 수정 이유
- 왜 이 작업이 필요한지

3. 테스트 결과
- 실행 또는 확인 결과

4. 확인 요청 사항
- 리뷰어가 중점적으로 볼 부분
```

### 작성 예시

```text
1. 작업 내용
- AST 분석에서 eval, exec, getattr 탐지 로직 추가
- 위험 함수 발견 시 경고 메시지 출력 기능 추가

2. 수정 이유
- 커스텀 코드 검증 단계에서 위험 함수 탐지가 필요함

3. 테스트 결과
- 정상 Python 파일 실행 시 이상 없음
- eval 포함 샘플 파일 탐지 성공

4. 확인 요청 사항
- getattr를 무조건 위험으로 처리한 기준이 적절한지 확인 부탁드립니다
```

---

## 6-7. PR 올린 뒤 해야 할 일

PR을 만들고 끝이 아니다. 아래까지 해야 한다.

1. 팀장에게 PR 확인 요청
2. GitHub의 리뷰 코멘트 확인
3. 수정 요청이 오면 다시 고친 뒤 commit/push
4. 승인 후 merge

즉, PR은 **제출**이 아니라 **검토 요청**이다.

---

## 6-8. 리뷰 요청받았을 때 확인 기준

리뷰어는 아래 기준으로 확인한다.

- 코드가 실행 가능한지
- 기존 기능을 깨뜨리지 않는지
- 불필요하게 복잡하지 않은지
- AI가 생성한 코드라도 의미 없이 복붙된 부분은 없는지
- 파일/함수 이름이 역할에 맞는지
- 보안 핵심 로직이면 위험한 우회 지점이 없는지

초보 팀에서는 모든 코드를 깊게 분석하기 어렵기 때문에, 최소한 아래 두 가지는 꼭 본다.

- **이 코드가 실제로 실행되는가**
- **작업 설명과 코드 내용이 일치하는가**

---

## 6-9. 리뷰 의견이 달렸을 때 수정 방법

리뷰 중 수정 요청이 오면 PR을 닫지 말고,  
**같은 브랜치에서 수정한 뒤 다시 commit/push** 하면 된다.

```bash
git checkout feature/ast-parser
git add .
git commit -m "[fix] analyzer: 리뷰 반영하여 getattr 예외 조건 추가"
git push origin feature/ast-parser
```

그러면 기존 PR에 자동으로 반영된다.

**새 PR을 만들 필요 없다.**

---

## 6-10. 승인 후 merge 규칙

우리 팀은 아래 규칙을 따른다.

- 승인 없이 merge 금지
- 자기 PR을 자기 혼자 바로 merge 금지
- 핵심 로직 수정은 팀장 확인 후 merge
- merge 대상은 기본적으로 `dev`
- `main` 브랜치는 최종 안정 버전만 반영

즉,

- 개인 작업 → `feature/*`
- 검토 후 병합 → `dev`
- 최종 발표/배포 직전 → `main`

---

## 6-11. PR 생성 후 충돌이 생긴 경우

다른 사람이 먼저 `dev`에 코드를 합치면 내 PR에서 충돌이 날 수 있다.

이 경우 다음 순서로 처리한다.

```bash
git checkout dev
git pull origin dev
git checkout feature/ast-parser
git merge dev
```

충돌 난 파일을 수정한 뒤:

```bash
git add .
git commit -m "[fix] analyzer: dev 병합 충돌 해결"
git push origin feature/ast-parser
```

그러면 기존 PR에 반영된다.

---

## 6-12. 우리 팀 PR 최소 기준

우리 팀은 아래 기준을 만족해야 PR merge 가능하다.

1. feature 브랜치에서 작업했을 것
2. 코드 실행 또는 기능 확인을 했을 것
3. PR 제목을 규칙에 맞게 작성했을 것
4. PR 본문 4개 항목을 작성했을 것
5. 팀장 또는 리뷰 담당자 확인을 받았을 것
6. 충돌이 해결된 상태일 것

---

# 7. Git 충돌을 줄이기 위한 모듈 작업 원칙

프로젝트는 아래와 같은 모듈 중심으로 나눠서 진행한다.

```text
proxy
analyzer
whitelist
sandbox
```

이 구조가 섞이면 Git 오류가 많아진다.

따라서 아래 원칙을 따른다.

1. 한 명이 한 모듈 중심으로 작업한다.
2. 동시에 같은 파일을 수정하지 않는다.
3. 공통 파일 수정이 필요하면 먼저 팀장에게 공유한다.
4. 폴더 구조 변경은 반드시 허락을 받고 진행한다.
5. 모듈 경계를 넘는 작업은 PR 본문에 이유를 적는다.

---

# 8. 개발 중부터 반드시 수집해야 하는 자료

개발이 끝난 뒤에 스크린샷, 로그, 회의록을 모으려고 하면 늦다.  
따라서 아래 폴더는 **구현 착수 시점부터 같이 채운다.**

## 8-1. 증빙 폴더 구조

```text
evidence/worklog/          # 작업 단계별 스크린샷, 로그, 데모 캡처
evidence/meetings/        # 회의록 PDF/MD, 참석자 기록
evidence/tests/           # pytest 결과, 수동 테스트 표, 회귀 테스트 로그
evidence/demo/            # 5개 시나리오 입력 파일, 실행 명령어, 출력 캡처
evidence/perf/            # 처리 시간, 비교 표, 성능 측정 원본
evidence/budget/          # 회의비, 재료비, 영수증 사본, 증빙 사진
```

---

## 8-2. 매주 반드시 남길 자료

매주 아래 자료를 남긴다.

- 회의록
- 주요 결정 기록(decision log)
- 아키텍처 변경 기록
- 테스트 결과
- 버그 및 수정 내역
- 성능 수치 표
- 데모 스크린샷/영상
- known limitations / false positive 정리
- 참고문헌 후보 목록

보고서·논문에 쓸 수 있는 그림과 표는 발견 즉시 따로 표시해 둔다.

---

## 8-3. 구현 안정화 시 인계 패키지

구현 안정화 단계까지 아래가 준비돼 있어야 한다.

```text
docs/release_notes_v1.0.md
docs/handoff_for_report_and_paper.md
docs/final_demo_script.md
docs/final_acceptance_sheet.md
README 최종판
실행 가이드
아키텍처 설명 자료
테스트 결과표
AI 사용 추적표
evidence index
```

개발 종료 직후 보고서·논문 단계로 넘어갈 수 있어야 하므로,  
**코드만 넘기면 안 되고 인계 패키지까지 완성**해야 한다.

---

# 9. 캡스톤 제출 관점에서 같이 챙길 문서

학교 제출 관점에서는 아래 문서가 중요하다.

- 결과보고서
- 정산 내역서
- 회의록
- 영수증 첨부지
- 변경 신청서(변경 시)
- 만족도 조사(결과보고서 제출 시)

따라서 개발팀 문서화와 학교 제출 서류 관리를 분리하지 말고  
**같은 흐름에서 동시에 관리**한다.

---

# 10. GitHub 저장소 권장 폴더 예시

아래는 운영을 쉽게 하기 위한 권장 구조다.

```text
project-root/
├─ proxy/
├─ analyzer/
├─ whitelist/
├─ sandbox/
├─ tests/
├─ docs/
│  ├─ release_notes_v1.0.md
│  ├─ handoff_for_report_and_paper.md
│  ├─ final_demo_script.md
│  └─ final_acceptance_sheet.md
├─ evidence/
│  ├─ worklog/
│  ├─ meetings/
│  ├─ tests/
│  ├─ demo/
│  ├─ perf/
│  └─ budget/
└─ README.md
```

---

# 11. 팀원이 실제로 따라야 하는 최소 행동 규칙

팀원은 아래만 지켜도 큰 사고를 줄일 수 있다.

1. 작업 전 `dev` 최신화
2. 무조건 `feature/*` 브랜치 생성 후 작업
3. AI 코드 바로 commit 금지
4. 직접 읽고, 실행하고, 영향 확인 후 commit
5. 커밋 메시지는 규칙대로 작성
6. GitHub에 push 후 `feature → dev` PR 생성
7. PR 본문 4개 항목 반드시 작성
8. 리뷰 요청 후 승인받고 merge
9. 증빙 자료를 매주 `evidence/`에 정리
10. 폴더 구조 변경은 사전 허락 후 진행

---

# 12. 팀장 확인 체크리스트

팀장은 PR 확인 시 아래를 본다.

## 코드 측면

- 실행 가능한가
- 기존 기능을 깨뜨리지 않는가
- 모듈 경계를 불필요하게 넘지 않았는가
- AI 생성 코드가 검증 없이 들어가지 않았는가
- 파일명/함수명이 역할에 맞는가

## 운영 측면

- 커밋 메시지가 규칙에 맞는가
- PR 제목/본문이 작성되었는가
- 테스트 결과가 적혀 있는가
- 증빙 자료가 같이 정리되고 있는가
- 핵심 로직 수정이면 별도 설명이 있는가

---

# 13. PR 본문 템플릿

아래 템플릿을 그대로 복붙해서 사용한다.

```text
1. 작업 내용
-

2. 수정 이유
-

3. 테스트 결과
-

4. 확인 요청 사항
-
```

---

# 14. 회의록 / AI 사용 기록 최소 템플릿

## 14-1. 회의록 최소 템플릿

```text
회의 일시:
참석자:
회의 안건:

결정 사항:
-

할 일:
- 담당자 / 작업 / 마감일

비고:
-
```

## 14-2. AI 사용 추적표 최소 항목

```text
날짜:
작업자:
작업 내용:
사용한 AI:
입력 요약:
출력 요약:
실제 반영 여부:
사람이 수정한 내용:
비고:
```

---

# 15. 최종 정리

이 문서의 핵심은 아래다.

- **작업은 feature 브랜치에서 한다**
- **dev로 PR을 올린다**
- **AI 코드는 검증 후 commit한다**
- **PR은 검수 절차다**
- **모듈 경계를 지켜 Git 충돌을 줄인다**
- **개발과 동시에 증빙 자료를 쌓는다**
- **코드뿐 아니라 인계 패키지까지 준비한다**

이 규칙을 지키면,

- Git 충돌이 줄고
- 팀장 검토가 쉬워지고
- AI 사용으로 인한 실수를 줄일 수 있으며
- 보고서·논문·캡스톤 제출까지 한 흐름으로 이어갈 수 있다.
